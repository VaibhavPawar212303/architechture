# .gitignore

# Terraform state files - These can contain sensitive data and are specific to your deployment.
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# The .terraform directory contains provider plugins and modules.
# It's generated by 'terraform init' and should not be committed.
.terraform/

# Terraform plan files can contain sensitive information.
*.tfplan

# Variable files for personal use (e.g., secrets).
# Commit a .tfvars.example file instead to show what variables are needed.
*.tfvars
*.tfvars.json

# Override files are for local development and should not be shared.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform CLI configuration files
.terraformrc
terraform.rc